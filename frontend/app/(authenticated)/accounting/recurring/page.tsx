"use client";

import PlaceholderPage from "@/components/layout/placeholder-page";

const breadcrumbs = [{ label: "Accounting" }, { label: "Recurring" }];

export default function Page() {
  return <PlaceholderPage title="Recurring" breadcrumbs={breadcrumbs} />;
}
