"use client";

import PlaceholderPage from "@/components/layout/placeholder-page";

const breadcrumbs = [{ label: "Settings" }, { label: "Lock Dates" }];

export default function Page() {
  return <PlaceholderPage title="Lock Dates" breadcrumbs={breadcrumbs} />;
}
