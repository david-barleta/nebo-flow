"use client";

import PlaceholderPage from "@/components/layout/placeholder-page";

const breadcrumbs = [{ label: "Settings" }, { label: "Approval Workflows" }];

export default function Page() {
  return <PlaceholderPage title="Approval Workflows" breadcrumbs={breadcrumbs} />;
}
