"use client";

import PlaceholderPage from "@/components/layout/placeholder-page";

const breadcrumbs = [{ label: "Settings" }, { label: "Setup Mode" }];

export default function Page() {
  return <PlaceholderPage title="Setup Mode" breadcrumbs={breadcrumbs} />;
}
